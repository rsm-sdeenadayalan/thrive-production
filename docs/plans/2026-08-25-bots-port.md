# Port the FAQ bot and Course Recommender Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Bring the upgraded **FAQ bot (`resources`)** and **course recommender (`courses`)** from the collaborator snapshot into thrive-production — and nothing else.

**Architecture:** The collaborator's repo (`../thrive-chatbots-friend`, read-only reference) is a *fork of an older thrive-production*. Their bots evolved; their jobs/resume/UI code is BEHIND ours. So this port is **additive cherry-picking**, never wholesale file copying. Two bots come over with their engines (hybrid retrieval + typo tolerance; deterministic planner), their data (corpus, catalog), their tests, and the chat-UI affordances the planner interview needs (quick-reply buttons + rating form).

**Spec:** This plan. Reference tree: `/Users/shankar/Documents/Rady Recommender/Consolidated/thrive-chatbots-friend` (READ ONLY — never write there, never `git` there).

## Verified before writing this plan (2026-08-25, by the controller)

- Their backend suite: **421 passed**.
- Golden eval with REAL TritonAI + their 259-doc corpus: **6/9** (3 failures are exact-string assertions the model paraphrased + one documented corpus gap; all 3 refusal cases pass).
- Planner live end-to-end: interview → **valid 50-unit plan**, correct 17-month skeleton (8/12/14/12), 13 unique courses, all four quarters, and it announced an assumed skill rating (documented escape hatch).
- FAQ live: `"How do i get zooom?"` → correct Zoom page (typo tolerance works); off-topic → `model_note="refusal"`, **0 chunks, no LLM call**.

## Global Constraints

- **IN SCOPE: exactly two bots.** FAQ (`resources`) and course recommender (`courses`), their engines, data, config, tests, and the chat-UI plumbing they require.
- **OUT OF SCOPE — never copy from the reference tree:** anything under `services/jobs/`, `views/jobs.py`, `models/jobs.py`, `data/jobs/companies.json`, `services/resume.py`, `views/resume.py`, `seed_demo.py`, `pipeline/`, `scripts/`, `docs/`, `static/`, `chat_cli.py`, `run_playground.py`, and **the career bot** (`answer_career` and its config/corpus destination). Ours are newer or deliberately different.
- **Never regress our work.** Our repo is ahead on: the Career job feed (M2 + two-step flow), resume latest-only upload, advisors (Amber Hanna / Nelitza Morales), the UI polish sweep (TaskRow, TasksCard, AddTaskForm, messages.ts copy, richtext, type ladder, bordered cards), and the removal of the `/ask/career` sub-tab. Where a file differs because OURS is newer, take only the bot-related additions into our version.
- `/ask` destinations stay **resources + courses** (career sub-tab stays removed from the UI). Our existing backend `answer_career` stays as-is, untouched.
- Migrations: their `0016`–`0022` collide with our `0016_postinginteraction_jobposting_content_hash`. Renumber theirs to `0017`+ in our tree, preserving dependency order.
- Secrets: TritonAI key only from `backend/.env` at runtime; never printed, never committed.
- Gates (run before every commit): `cd backend && uv run pytest -q` and `cd frontend && npm test && npm run check && npm run check:layout`, plus `python3 scripts/check-contrast.py` from the repo root when CSS changes.
- Copy stays in `frontend/src/lib/messages.ts`; design-system rules hold (no hex/font-*/dark:/new `thrive-*` classes without registering them).

---

### Task 1: Retrieval engine + chunking + corpus data

**Files:**
- Overwrite from reference (bot-owned, no local divergence): `backend/rsm_thrive/services/retrieval.py`
- Merge additions: `backend/rsm_thrive/services/ingest.py` (heading-aware `chunk_text`), `backend/rsm_thrive/management/commands/ingest_corpus.py` (`source_url`, `destinations_for`, `--catalog`)
- Copy data: `backend/rsm_thrive/data/corpus/**` (259 docs, ~1.7MB), `backend/rsm_thrive/data/evals/faq_golden.json`, `backend/rsm_thrive/data/catalog/careers.json` (diff first; take theirs only if the planner needs it)
- Model: `backend/rsm_thrive/models/knowledge.py` — add `Document.source_url`
- Migration: renumber their `0016_document_source_url` → `0017_document_source_url`
- Tests: `tests/test_retrieval.py`, `tests/test_ingest.py`, `tests/fixtures/faq_golden_fixture.json` from reference

**Interfaces produced:** `retrieve(question, destination, top_k, min_similarity, lexical_min, lexical_floor)`; hybrid rank = `cosine + 0.25 × keyword`; two admission tiers; edit-distance-1 typo repair for ≥5-char terms absent from the corpus.

- [ ] Steps: copy/merge → renumber migration → `uv run python manage.py makemigrations --check` clean → backend suite green (baseline 273, will grow) → commit.

### Task 2: The FAQ bot

**Files:**
- Merge into `backend/rsm_thrive/services/bots.py`: `BotReply` dataclass (body, chunk_ids, model_note, quick_replies, form), `build_context`, `append_sources`, `answer_faq`. **KEEP our `answer_career` exactly as it is.** Adapt call sites if our `answer_career` returned a bare string — it may now need to return `BotReply(body=...)` for a uniform view layer.
- `backend/config/bots.json`: take their `faq` block (top_k 10, min_similarity 0.55, lexical_min 1.0, lexical_floor 0.4, max_history_turns 12). Leave `career` as ours.
- `backend/rsm_thrive/services/bot_config.py` if the config shape changed.
- Tests: `tests/test_bots_faq.py`, `tests/test_eval_command.py`, `management/commands/eval_bots.py` from reference.

**Interfaces produced:** `answer_faq(llm, question, history) -> BotReply`; deterministic refusal with **no LLM call** when retrieval is empty.

- [ ] Steps: merge → backend suite green → `THRIVE_LLM=fake uv run python manage.py eval_bots` runs (fake-calibrated failures acceptable and expected; assert the command executes and refusal cases pass) → commit.

### Task 3: Planner engine, models, and API

**Files:**
- Copy: `backend/rsm_thrive/services/planner.py`, `backend/rsm_thrive/models/planner.py`, `backend/rsm_thrive/views/planner.py`
- Merge: `backend/rsm_thrive/models/__init__.py` (export PlannerSession/CoursePlan), `backend/rsm_thrive/urls.py` (add `/plan`, `/plan/intake`, `/plan/alternatives`, `/plan/swap` — keep every route we already have), `backend/rsm_thrive/services/electives.py` if their `rank_electives` differs
- Add `answer_electives` to `services/bots.py`
- Migrations: renumber their `0017_courseplan`, `0018_alter_courseplan_track`, `0019_plannersession`, `0022_plannersession_review` → `0018`–`0021` in our tree, dependencies rewritten to match
- Tests: `tests/test_planner.py`, `tests/test_planner_api.py`, `tests/test_bots_electives.py` from reference

**Interfaces produced:** `answer_electives(llm, conversation, question, history) -> BotReply`; `TRACK_SKELETONS` (11-month and 17-month, totalling 50 units); `alternatives_for`, `apply_swap` with skeleton validation and elective pinning.

- [ ] Steps: copy/merge → `makemigrations --check` clean → `migrate` on a scratch DB → backend suite green → commit.

### Task 4: Chat write path — quick replies and forms

**Files:**
- Merge: `backend/rsm_thrive/models/chat.py` (`ChatMessage.quick_replies`, `ChatMessage.form`), `backend/rsm_thrive/serializers/chat.py`, `backend/rsm_thrive/views/chat.py` (persist and return both; keep our degraded-reply-never-500 behaviour)
- Migrations: renumber their `0020_chatmessage_quick_replies`, `0021_chatmessage_form` → next free numbers
- Tests: `tests/test_chat_write.py`, `tests/contract/schemas.py` additions from reference

- [ ] Steps: merge → `makemigrations --check` clean → backend suite green → commit.

### Task 5: Frontend — buttons, rating form, and wiring

**Files (ADDITIVE ONLY — our polish must survive):**
- `frontend/src/lib/data/types.ts` — add `quickReplies`/`form` to the chat message type
- `frontend/src/lib/data/api/providers.ts` + `providers.ts` (mock) — carry the new fields
- `frontend/src/lib/components/ask/ChatWindow.svelte` — render quick-reply buttons and the 1–5 rating form; posting a choice sends it as the next student turn
- `frontend/src/lib/components/ask/AskHistory.svelte`, `RichMessage.svelte` — only if the reference adds something the planner output needs (tables!). **Our `richtext.ts` markdown subset must render the planner's quarter TABLES** — if it cannot, extend it (with tests) rather than importing their version wholesale.
- `frontend/src/lib/messages.ts` — new copy keys only, never overwrite our polished copy
- **Do NOT touch** `ask.ts`/`ask.spec.ts` destination lists (career stays removed), TaskRow, TasksCard, AddTaskForm, or any job/appointments file.
- Tests: extend our specs; take reference test cases where they apply.

- [ ] Steps: implement → `npm test`, `npm run check`, `npm run check:layout` green → commit.

### Task 6: Live verification and docs

- [ ] Ingest the corpus into our dev DB with the real key: `for d in crawled canvas program; do uv run python manage.py ingest_corpus rsm_thrive/data/corpus/$d; done; uv run python manage.py ingest_corpus --catalog` — expect ~259 docs / ~2,444 chunks at 1024 dims. **Job postings must survive** (count before/after).
- [ ] `uv run python manage.py eval_bots` with the real key: record the score (controller measured 6/9 in the reference tree; parity is the bar).
- [ ] Live planner: run the 5-turn interview through `answer_electives`; assert a 50-unit plan with all quarters.
- [ ] Live FAQ: `"How do i get zooom?"` answers from the Zoom page; an off-topic question returns `model_note="refusal"` with 0 chunks.
- [ ] Browser check via the running dev server: `/ask/resources` answers a real question; `/ask/courses` runs the interview with buttons and the rating form and prints a plan table.
- [ ] Update `backend/README.md` (bots section: retrieval tiers, corpus, planner, `/plan` API) and credit the collaborator's repo for the two bots.
- [ ] All gates green; report the honest numbers.
